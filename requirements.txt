fastapi>=0.111.0
nltk>=3.8
num2words>=0.5.13
uvicorn[standard]>=0.29.0
yt-dlp>=2024.4.9
openai-whisper>=20231117
stable-ts>=2.17.0
sqlalchemy>=2.0.0
python-multipart>=0.0.9
# Corpus bundles. `corpus.py pack` writes .tar.zst by default, and the
# container installs any bundle dropped in packs/ on start -- without this it
# could produce an archive format it could not read back, which is exactly
# what happened: a pack built on a workstation failed to install on the server.
zstandard>=0.22.0
